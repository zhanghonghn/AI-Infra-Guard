# Garak adapter 依赖
# 核心：Garak LLM 安全扫描框架（**生产环境必装**）
# adapter 通过子进程方式调用 Garak 官方 CLI（python -m garak）
garak>=0.14.0

# 说明：
# - 真实模式（默认）：调用 garak CLI，解析 *.report.jsonl 报告
# - Mock 模式（仅 CI/烟测）：环境变量 GARAK_MOCK=1 或 main.py --mock 显式开启
# - garak 未安装时，真实模式会立即抛错而非静默降级，避免误导性结果
